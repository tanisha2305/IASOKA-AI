Install requirements:

bash


pip install streamlit requests
Run the Streamlit application:

bash


streamlit run frontend_app.py
Open in your browser: It will automatically open at 👉 http://localhost:8501.

The file is also saved on disk at: 
frontend_app.py
.
